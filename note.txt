pip freeze > requirements.txt

Chỉ test + tăng version + build + nén zip, nhưng chưa upload/push.
python.exe check.py --publish

python check.py

python check.py
 = full publish luôn.


python check.py --local
 = chỉ test/build local, không push.

python check.py --no-bump --no-build
 = kiểm tra nhanh.